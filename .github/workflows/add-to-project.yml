name: Add to Project

on:
  issues:
    types: [opened]

jobs:
  add-to-project:
    uses: kk-house-777-lab/kk-repo/.github/workflows/add-to-project.yml@main
    with:
      project-url: https://github.com/orgs/kk-house-777-lab/projects/1
    secrets:
      project-token: ${{ secrets.ADD_TO_PROJECT_PAT }}
